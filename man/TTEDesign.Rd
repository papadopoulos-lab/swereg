% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tte_classes.R
\name{TTEDesign}
\alias{TTEDesign}
\title{TTEDesign class for target trial emulation}
\usage{
TTEDesign(
  person_id_var = character(0),
  id_var = "trial_id",
  exposure_var = character(0),
  outcome_vars = character(0),
  confounder_vars = character(0),
  follow_up_time = integer(0),
  tstart_var = "tstart",
  tstop_var = "tstop",
  time_exposure_var = character(0),
  eligible_var = character(0),
  admin_censor_var = character(0),
  admin_censor_isoyearweek = character(0)
)
}
\arguments{
\item{person_id_var}{Character or NULL, name of the person identifier column
for pre-panel (person-week) data. Required for [tte_match()] and
[tte_expand()] operations. (default: NULL).}

\item{id_var}{Character, name of the trial identifier column. Auto-generated
by [tte_expand()] as "trial_id" (default: "trial_id").}

\item{exposure_var}{Character, name of the baseline exposure/treatment column.}

\item{outcome_vars}{Character vector, names of outcome event indicator columns.}

\item{confounder_vars}{Character vector, names of confounder columns for
propensity/censoring models.}

\item{follow_up_time}{Integer, expected follow-up duration in time units.}

\item{tstart_var}{Character, name of period start time column (default: "tstart").}

\item{tstop_var}{Character, name of period end time column (default: "tstop").}

\item{time_exposure_var}{Character or NULL, name of time-varying exposure column
for per-protocol analysis (default: NULL).}

\item{eligible_var}{Character or NULL, name of eligibility indicator column
(default: NULL).}

\item{admin_censor_var}{Character or NULL, name of administrative censoring
boundary column (default: NULL). Mutually exclusive with
`admin_censor_isoyearweek`.}

\item{admin_censor_isoyearweek}{Character or NULL, the study end date in
ISO year-week format (e.g., "2023-52"). When set, administrative censoring
is computed internally as weeks from each trial's entry date to this
global study end date. Requires an `isoyearweek` column in the data.
Mutually exclusive with `admin_censor_var` (default: NULL).}
}
\description{
Holds column name mappings that define the schema for trial data. This
allows specifying variable names once and reusing them across all TTE
workflow functions.
}
\examples{
# Design for post-panel (trial-level) data
design <- tte_design(
  id_var = "trial_id",
  exposure_var = "baseline_exposed",
  outcome_vars = c("death", "hosp"),
  confounder_vars = c("age", "education"),
  follow_up_time = 156L
)

# Design for pre-panel (person-week) data with full workflow
design_prepanel <- tte_design(
  person_id_var = "id",
  exposure_var = "baseline_exposed",
  outcome_vars = c("death", "hosp"),
  confounder_vars = c("age", "education"),
  follow_up_time = 156L,
  eligible_var = "eligible"
)

}
\seealso{
[tte_trial()] for creating trial objects, [TTETrial] for the trial class

Other tte_classes: 
\code{\link{TTEPlan}()},
\code{\link{TTETrial}()},
\code{\link{tte_design}()},
\code{\link{tte_plan}()},
\code{\link{tte_plan_add_one_ett}()},
\code{\link{tte_plan_task}()},
\code{\link{tte_trial}()}
}
\concept{tte_classes}
