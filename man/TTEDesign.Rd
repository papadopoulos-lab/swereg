% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r6_ttedesign.R
\name{TTEDesign}
\alias{TTEDesign}
\title{TTEDesign class for target trial emulation}
\description{
Holds column name mappings that define the schema for trial data. This
allows specifying variable names once and reusing them across all TTE
workflow functions.

swereg 26.9.0 moved time zero to the landmark. A \code{tstart == 0}
row of a schema-2 object is an entry band row, and a 26.9.0 reader
takes it for a landmark row. The check refuses the object, so that
reinterpretation cannot happen in silence.
}
\section{The interval convention}{


Every interval is \verb{[tstart, tstop)}. The stop is exclusive. The person
leaves the risk set at \code{tstop}, and the row holds no part of that week.

Every duration is \code{tstop - tstart}. It never adds one. Three complete
four-week bands span \verb{[0, 12)}. That is 12 person-weeks, and the bands bill
4, 4 and 4. The inclusive convention bills 5, 5 and 5.

Every \verb{weeks_to_*} column is a boundary on the same scale, counted from the
landmark at week 0. \code{weeks_to_event}, \code{weeks_to_protocol_deviation},
\code{weeks_to_loss}, \code{weeks_to_admin_end} and \code{weeks_to_record_end} each name
the first week the person no longer contributes. A \code{weeks_to_record_end} of
9 means the person held follow-up weeks 1 to 9 and bills 9 person-weeks.

The \code{+ 1} belongs to the inclusive convention, where weeks 1 through 4 is
\code{4 - 1 + 1 = 4}. Both are correct arithmetic. The two differ in whether the
stop belongs to the interval. A mix of them makes a silently wrong
denominator, so swereg MUST read every stop as exclusive.

One place adds a week, and it converts a calendar reading into a stop.
\code{admin_censor_isoyearweek} names the last week under study, and
\code{difftime()} returns the whole weeks between that week and the landmark
week. The stop is one week later, because the person holds the whole of the
administrative week.

\code{tests/testthat/test-interval-convention.R} pins each of the five
boundaries.
}

\examples{
# Design for post-panel (trial-level) data
design <- TTEDesign$new(
  id_var = "enrollment_person_trial_id",
  treatment_var = "baseline_intervention",
  outcome_vars = c("death", "hosp"),
  confounder_vars = c("age", "education"),
  follow_up_time = 156L
)

# Design for pre-panel (person-week) data with full workflow
design_prepanel <- TTEDesign$new(
  person_id_var = "id",
  treatment_var = "baseline_intervention",
  outcome_vars = c("death", "hosp"),
  confounder_vars = c("age", "education"),
  follow_up_time = 156L,
  eligible_var = "eligible",
  observed_var = list(column = "rd_observed")
)

# The same design on a trimmed skeleton. A row exists if and only if the
# person was observed that week, so there is no column to name.
design_trimmed <- TTEDesign$new(
  person_id_var = "id",
  treatment_var = "baseline_intervention",
  outcome_vars = c("death", "hosp"),
  confounder_vars = c("age", "education"),
  follow_up_time = 156L,
  eligible_var = "eligible",
  observed_var = list(sentinel = "row_presence")
)

}
\seealso{
\link{TTEEnrollment} for the trial class.
\code{vignette("tte-nomenclature")} for the enrollment band vocabulary.

Other tte_classes:
\code{\link{TTEEnrollment}},
\code{\link{TTEPlan}}
}
\concept{tte_classes}
\section{Public fields}{
  \if{html}{\out{<div class="r6-fields">}}
  \describe{
    \item{\code{person_id_var}}{Character or NULL, person identifier column name.}

    \item{\code{id_var}}{Character, person-trial identifier column name.}

    \item{\code{treatment_var}}{Character, treatment column name. Enrollment reads
every eligible week of the entry band, not only its first week. See
the Baseline treatment section of TTEEnrollment for the full rule.}

    \item{\code{outcome_vars}}{Character vector, outcome column names.}

    \item{\code{confounder_vars}}{Character vector, confounder column names.}

    \item{\code{subgroup_vars}}{Character vector or NULL, baseline subgroup
(effect-modifier) column names; should be a subset of confounder_vars.}

    \item{\code{follow_up_time}}{Integer, follow-up duration.}

    \item{\code{tstart_var}}{Character, period start time column name.}

    \item{\code{tstop_var}}{Character, period end time column name.}

    \item{\code{time_treatment_var}}{Character or NULL, time-varying treatment column.}

    \item{\code{eligible_var}}{Character or NULL, eligibility column name.}

    \item{\code{observed_var}}{The observation encoding, or NULL. It is a
\code{tte_observed_var} list with a \code{column} and a \code{sentinel}, exactly one
of which is set. \code{column} names a real logical person-week column.
\code{sentinel} of \code{"row_presence"} asserts a trimmed skeleton, where a
row exists if and only if the person was observed that week.}

    \item{\code{intervention_tolerance_weeks}}{Integer, the tolerance in weeks for
the intervention arm.}

    \item{\code{comparator_tolerance_weeks}}{Integer, the tolerance in weeks for
the comparator arm.}

    \item{\code{admin_censor_var}}{Character or NULL, admin censoring column.}

    \item{\code{admin_censor_isoyearweek}}{Character or NULL, admin censoring date.}

    \item{\code{period_width}}{Integer, band width in weeks for enrollment and
aggregation. Eligibility and treatment status are assessed weekly.
\code{period_width} collapses consecutive weeks into bands, and each band
opens exactly one trial. Initiation in any week of a band is
attributed to the start of that band.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-TTEDesign-initialize}{\code{TTEDesign$new()}}
    \item \href{#method-TTEDesign-check_version}{\code{TTEDesign$check_version()}}
    \item \href{#method-TTEDesign-print}{\code{TTEDesign$print()}}
    \item \href{#method-TTEDesign-clone}{\code{TTEDesign$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TTEDesign-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-TTEDesign-initialize}{}}}
\subsection{\code{TTEDesign$new()}}{
  Create a new TTEDesign object.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TTEDesign$new(
  person_id_var = "id",
  id_var = "enrollment_person_trial_id",
  treatment_var,
  outcome_vars,
  confounder_vars,
  follow_up_time,
  subgroup_vars = NULL,
  tstart_var = "tstart",
  tstop_var = "tstop",
  time_treatment_var = NULL,
  eligible_var = NULL,
  observed_var = NULL,
  intervention_tolerance_weeks = 0L,
  comparator_tolerance_weeks = 0L,
  admin_censor_var = NULL,
  admin_censor_isoyearweek = NULL,
  period_width = 4L
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{person_id_var}}{Character or NULL, name of the person identifier column
(default: \code{"id"}). \code{create_skeleton()} names the person identifier \code{id},
and \code{TTEPlan} passes \code{"id"} whenever an argset does not override it, so the
default matches what the pipeline already builds. A person contributes many
sequential trials, so this column is what separates a head count of people
from a count of person-trials.}
      \item{\code{id_var}}{Character, name of the person-trial identifier column (default: "enrollment_person_trial_id").}
      \item{\code{treatment_var}}{Character, name of the baseline treatment column. It
holds \code{TRUE} for the intervention arm, \code{FALSE} for the comparator arm, and
\code{NA} outside the two arms. Enrollment reads every eligible week of the
entry band, not only its first week. See the Baseline treatment section
of TTEEnrollment for the full rule.}
      \item{\code{outcome_vars}}{Character vector, names of outcome event indicator columns.}
      \item{\code{confounder_vars}}{Character vector, names of confounder columns for
propensity/censoring models.}
      \item{\code{follow_up_time}}{Integer, expected follow-up duration in time units.}
      \item{\code{subgroup_vars}}{Character vector or NULL, baseline subgroup
(effect-modifier) column names; should be a subset of confounder_vars.}
      \item{\code{tstart_var}}{Character, name of period start time column (default: "tstart").}
      \item{\code{tstop_var}}{Character, name of period end time column (default: "tstop").}
      \item{\code{time_treatment_var}}{Character or NULL, name of time-varying treatment column
for per-protocol analysis (default: NULL).}
      \item{\code{eligible_var}}{Character or NULL, name of eligibility indicator column
(default: NULL).}
      \item{\code{observed_var}}{The observation encoding, or NULL (default: NULL). It
states how the data records that a person was under observation in a
week. Give a list with exactly one of two keys.
\code{list(column = "rd_observed")} names a real logical person-week column.
\code{list(sentinel = "row_presence")} asserts that the caller already deleted
every unobserved person-week. A row then exists if and only if the person
was observed that week. Person-week data MUST declare one of the two
forms. Already-expanded trial data MAY leave this NULL. One row there is
one trial, and not one week of observation.}
      \item{\code{intervention_tolerance_weeks}}{Integer, the tolerance in weeks for the
intervention arm (default: 0L). It MUST be a whole number of at least 0.}
      \item{\code{comparator_tolerance_weeks}}{Integer, the tolerance in weeks for the
comparator arm (default: 0L). It MUST be a whole number of at least 0.}
      \item{\code{admin_censor_var}}{Character or NULL, name of administrative censoring
boundary column (default: NULL). Mutually exclusive with
\code{admin_censor_isoyearweek}. Not implemented in outcome preparation:
\code{s5_prepare_outcome()} stops if this is set -- use
\code{admin_censor_isoyearweek} instead.}
      \item{\code{admin_censor_isoyearweek}}{Character or NULL, the study end date in
ISO year-week format (e.g., "2023-52"). When set, administrative censoring
is computed internally as weeks from each trial's entry date to this
global study end date. Requires an \code{isoyearweek} column in the data.
Mutually exclusive with \code{admin_censor_var} (default: NULL).}
      \item{\code{period_width}}{Integer, band width in weeks for enrollment and
time aggregation (default: 4L). The input is a person-week skeleton, so
eligibility and treatment status are assessed weekly. \code{period_width} then
collapses consecutive weeks into bands, and each band opens exactly one
trial. With \code{period_width = 4L}, one trial opens every four weeks, not one
trial per week. Initiation in any week of a band is attributed to the
start of that band. Must be a positive integer.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TTEDesign-check_version"></a>}}
\if{latex}{\out{\hypertarget{method-TTEDesign-check_version}{}}}
\subsection{\code{TTEDesign$check_version()}}{
  Check this object's schema version against the current
class version. It stops when the object carries an older schema.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TTEDesign$check_version()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{invisible(TRUE)} when the versions match. It stops otherwise.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TTEDesign-print"></a>}}
\if{latex}{\out{\hypertarget{method-TTEDesign-print}{}}}
\subsection{\code{TTEDesign$print()}}{
  Print the TTEDesign object.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TTEDesign$print(...)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{...}}{Ignored.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TTEDesign-clone"></a>}}
\if{latex}{\out{\hypertarget{method-TTEDesign-clone}{}}}
\subsection{\code{TTEDesign$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TTEDesign$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
