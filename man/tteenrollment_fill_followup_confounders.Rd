% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tteenrollment_fill.R
\name{tteenrollment_fill_followup_confounders}
\alias{tteenrollment_fill_followup_confounders}
\title{Carry the last observed confounder value forward through follow-up}
\usage{
tteenrollment_fill_followup_confounders(trial)
}
\arguments{
\item{trial}{A \link{TTEEnrollment} object at trial level.}
}
\value{
The \link{TTEEnrollment} object, invisibly.
}
\description{
The censoring model needs a value on every follow-up row. This function
fills each plain confounder column of a trial panel. It carries the last
observed value forward within each person-trial.
}
\details{
The carry starts from the entry-window snapshot, so the first row of a
person-trial is never left \code{NA}. \verb{$s1_impute_confounders()} supplies that
snapshot value for a person-trial with no observed value before follow-up.

The function reads \code{trial$design} for every column name. It fills only the
confounders that carry a \code{.tte_entry__} snapshot column. A panel with no
snapshot column at all comes back untouched.

The function sorts \code{trial$data} by \code{id_var}, then \code{tstart_var}, then
\code{tstop_var}. It modifies \code{trial$data} by reference. It appends
\code{"fill_followup"} to \code{trial$steps_completed} once. A second call appends no
second token and changes no value.

The function stops when a filled row would still hold \code{NA}. That happens
when the snapshot is \code{NA} and no earlier row of the same person-trial holds
a value.
}
\examples{
d <- data.table::data.table(
  enrollment_person_trial_id = rep(c("a", "b"), each = 3L),
  tstart = rep(c(0L, 4L, 8L), 2L),
  tstop = rep(c(4L, 8L, 12L), 2L),
  exposed = rep(c(TRUE, FALSE), each = 3L),
  event = 0L,
  income = c(NA, "mid", NA, NA, NA, "high"),
  .tte_entry__income = rep(c("low", "mid"), each = 3L)
)
design <- TTEDesign$new(
  treatment_var = "exposed",
  outcome_vars = "event",
  confounder_vars = "income",
  follow_up_time = 12L
)
trial <- TTEEnrollment$new(d, design)
tteenrollment_fill_followup_confounders(trial)
trial$data$income

}
\seealso{
\code{\link[=tteenrollment_fill_aggregates]{tteenrollment_fill_aggregates()}} for the counts the fill changes.

Other tte_methods:
\code{\link[=tteenrollment_combined_combine]{tteenrollment_combined_combine()}},
\code{\link[=tteenrollment_fill_aggregates]{tteenrollment_fill_aggregates()}},
\code{\link[=tteenrollment_fill_summary]{tteenrollment_fill_summary()}},
\code{\link[=tteenrollment_irr_combine]{tteenrollment_irr_combine()}},
\code{\link[=tteenrollment_rates_combine]{tteenrollment_rates_combine()}},
\code{\link[=tteenrollment_rbind]{tteenrollment_rbind()}}
}
\concept{tte_methods}
