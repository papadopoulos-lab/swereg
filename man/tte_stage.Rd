% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tte_stage.R
\name{tte_stage}
\alias{tte_stage}
\title{Run one target trial emulation pipeline stage}
\usage{
tte_stage(stage, dir_tteplan, ...)
}
\arguments{
\item{stage}{One of \code{"s1"}, \code{"s2"} or \code{"s3"}.}

\item{dir_tteplan}{Character vector of candidate directories, in priority
order, where \code{tteplan.qs2} lives. Passed to
\code{\link[=tteplan_locate_and_load]{tteplan_locate_and_load()}}, which takes the first one that exists.}

\item{...}{Arguments for the stage method. Each one MUST be named.}
}
\value{
The \link{TTEPlan}, invisibly.
}
\description{
Loads the plan in \code{dir_tteplan}, runs the stage method, then runs the steps
that follow it. This is the body of the per-project \code{s1.R}, \code{s2.R} and
\code{s3.R} stage scripts as one call.
}
\details{
The stage id selects the method and the steps after it:\tabular{lll}{
   \code{stage} \tab method \tab steps after the method \cr
   \code{"s1"} \tab \verb{$s1_generate_enrollments_and_ipw()} \tab \verb{$save()}, then \verb{$print_target_checklist()} \cr
   \code{"s2"} \tab \verb{$s2_generate_analysis_files_and_ipcw_pp()} \tab n/a \cr
   \code{"s3"} \tab \verb{$s3_analyze()} \tab \verb{$results_summary()}, then \verb{$save()} \cr
}


Every element of \code{...} MUST carry a name. \code{tte_stage()} matches each name
against the formals of the stage method, and forwards by name.

The three methods take their arguments in different orders, and none of them
takes \code{...}. A positional forward binds the wrong formal and reports no
error. Naming every argument is what makes the forward safe.
}
\section{Why an unknown name fails early}{


\code{tte_stage()} rejects a name that the stage method does not declare BEFORE
it calls \code{\link[=tteplan_locate_and_load]{tteplan_locate_and_load()}}. That load reads the plan from a
network share and is slow. A mistyped argument name therefore costs no
load. \code{do.call()} would also reject the name, but only after the load.

The formals come from the \link{TTEPlan} generator, so the check needs no plan.
}

\examples{
\dontrun{
swereg::tte_stage(
  "s1",
  "~/plans/003-iliadis-stroke",
  n_workers = 6L,
  swereg_dev_path = NULL
)
}
}
\seealso{
\code{\link[=tteplan_locate_and_load]{tteplan_locate_and_load()}}, \link{TTEPlan}.
\code{vignette("tte-workflow")} describes the stage scripts.

Other tte_plan:
\code{\link[=registrystudy_load]{registrystudy_load()}},
\code{\link[=tteplan_load]{tteplan_load()}},
\code{\link[=tteplan_locate_and_load]{tteplan_locate_and_load()}}
}
\concept{tte_plan}
