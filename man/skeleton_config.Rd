% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skeleton_classes.R
\name{skeleton_config}
\alias{skeleton_config}
\title{Create a skeleton pipeline configuration}
\usage{
skeleton_config(
  rawbatch_dir,
  group_names,
  skeleton_dir = rawbatch_dir,
  batch_sizes = c(1000L, 10000L),
  seed = 4L,
  id_col = "lopnr",
  ids_per_skeleton_file = 1000L
)
}
\arguments{
\item{rawbatch_dir}{Character vector of candidate paths for rawbatch files.
The first existing directory is used. If only one path is given and it
doesn't exist, it is accepted as-is (will be created).}

\item{group_names}{Character vector, names of rawbatch groups.}

\item{skeleton_dir}{Character vector of candidate paths for skeleton output.
Defaults to the same resolved value as `rawbatch_dir`.}

\item{batch_sizes}{Integer vector. First element = IDs in batch 1 (dev),
second = IDs in remaining batches. Default: `c(1000L, 10000L)`.}

\item{seed}{Integer, shuffle seed. Default: `4L`.}

\item{id_col}{Character, person ID column name. Default: `"lopnr"`.}

\item{ids_per_skeleton_file}{Integer, IDs per skeleton sub-file.
Default: `1000L`.}
}
\value{
A [SkeletonConfig] object.
}
\description{
Constructor function for [SkeletonConfig] objects. Accepts multiple candidate
paths (like [org::initialize_project()]) and resolves each to the first
existing directory at runtime.
}
\examples{
\dontrun{
config <- skeleton_config(
  rawbatch_dir = c(
    "//argos.rudbeck.uu.se/MyGroups$/Bronze/Embla_data/_MHT/2026/generic/",
    "/data/argos/Bronze/Embla_data/_MHT/2026/generic/",
    "C:/Users/isbeihm/Uppsala/argos/Bronze/Embla_data/_MHT/2026/generic/"
  ),
  group_names = c("lmed", "diagnoses_and_operations", "other")
)
}

}
\seealso{
[SkeletonConfig] for class details

Other skeleton_classes: 
\code{\link{SkeletonConfig}()},
\code{\link{SkeletonMeta}()},
\code{\link{skeleton_meta}()}
}
\concept{skeleton_classes}
