% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path_resolution.R
\name{first_existing_path}
\alias{first_existing_path}
\title{First candidate path that exists}
\usage{
first_existing_path(candidates, label = NULL)
}
\arguments{
\item{candidates}{Character vector of candidate paths, in priority order.}

\item{label}{Optional label used in error messages to describe what kind
of path is being resolved (e.g. `"data_rawbatch_dir"`).}
}
\value{
A single character path: the first candidate that exists, or a
  newly-created one.
}
\description{
Returns the first element of `candidates` for which [dir.exists()] is
`TRUE`. If none exist but one has a parent directory that exists, creates
that candidate and returns it. Errors if none of the candidates or their
parents exist.
}
\details{
This is the stateless primitive used by [CandidatePath] for resolution.
Scripts that just need "give me the first of these paths that exists" can
call it directly.
}
\examples{
d <- tempfile()
dir.create(d)
first_existing_path(c("/definitely/not/there", d))
}
\seealso{
[CandidatePath] for the stateful, caching wrapper used by the R6
  classes in this package; [invalidate_candidate_paths()] for the
  save-time cache clearer that makes objects portable across hosts.

Other multi_host_paths: 
\code{\link{CandidatePath}},
\code{\link{invalidate_candidate_paths}()}
}
\concept{multi_host_paths}
