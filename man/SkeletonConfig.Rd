% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skeleton_classes.R
\name{SkeletonConfig}
\alias{SkeletonConfig}
\title{SkeletonConfig class for skeleton pipeline}
\usage{
SkeletonConfig(
  rawbatch_dir = character(0),
  skeleton_dir = character(0),
  group_names = character(0),
  batch_sizes = integer(0),
  seed = integer(0),
  id_col = character(0),
  ids_per_skeleton_file = integer(0)
)
}
\arguments{
\item{rawbatch_dir}{Character, resolved single path for rawbatch files.}

\item{skeleton_dir}{Character, resolved single path for skeleton output.}

\item{group_names}{Character vector, names of rawbatch groups
(e.g., `c("lmed", "diagnoses_and_operations", "other")`).}

\item{batch_sizes}{Integer vector, IDs per batch. First element is used for
batch 1 (development batch), second element for all remaining batches.
Default: `c(1000L, 10000L)`.}

\item{seed}{Integer, shuffle seed for reproducibility. Default: `4L`.}

\item{id_col}{Character, name of the person ID column. Default: `"lopnr"`.}

\item{ids_per_skeleton_file}{Integer, number of IDs per skeleton sub-file.
Default: `1000L`.}
}
\description{
Holds directory paths, group names, batch sizing, and other configuration
for the rawbatch/skeleton pipeline. Analogous to [TTEDesign] -- specifies
the "schema" without holding data.
}
\section{Computed properties}{

\describe{
  \item{meta_file}{Character (read-only). Full path to the
    `skeleton_meta.qs2` file, derived from `rawbatch_dir`.}
}
}

\examples{
\dontrun{
config <- skeleton_config(
  rawbatch_dir = c(
    "/data/argos/Bronze/Embla_data/_MHT/2026/generic/",
    "C:/Users/isbeihm/Uppsala/argos/Bronze/Embla_data/_MHT/2026/generic/"
  ),
  group_names = c("lmed", "diagnoses_and_operations", "other")
)
}

}
\seealso{
[skeleton_meta()] for creating runtime objects

Other skeleton_classes: 
\code{\link{SkeletonMeta}()},
\code{\link{skeleton_config}()},
\code{\link{skeleton_meta}()}
}
\concept{skeleton_classes}
