% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path_resolution.R
\name{invalidate_candidate_paths}
\alias{invalidate_candidate_paths}
\title{Invalidate every CandidatePath cache inside an R6 object}
\usage{
invalidate_candidate_paths(obj)
}
\arguments{
\item{obj}{An R6 object to walk.}
}
\value{
\code{invisible(obj)}.
}
\description{
Walks the public fields of \code{obj} depth-first. Every field that is a
\link{CandidatePath} has its cache cleared via \verb{$invalidate()}. Every field that
is another R6 object is recursed into, so embedded objects (e.g. a
\link{RegistryStudy} held inside a \link{TTEPlan}) are also invalidated.
}
\details{
Active bindings are deliberately skipped during the walk: accessing a
\verb{$dir_foo} active binding would call \verb{$resolve()} on the underlying
\link{CandidatePath} and immediately re-populate the cache we are trying to
clear. We reach the \link{CandidatePath} instances via their backing public
fields instead.

This is called from \verb{$save()} on both \link{RegistryStudy} and \link{TTEPlan} before
serialization, so the on-disk qs2 file never carries host-specific resolved
paths. After loading on a different host, first access re-walks the
candidate list and caches the path valid on that host.
}
\seealso{
\link{CandidatePath}, \code{\link[=first_existing_path]{first_existing_path()}}, \link{RegistryStudy},
\link{TTEPlan}.

Other multi_host_paths:
\code{\link{CandidatePath}},
\code{\link[=first_existing_path]{first_existing_path()}}
}
\concept{multi_host_paths}
