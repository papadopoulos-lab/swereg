% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tteenrollment_fill.R
\name{tteenrollment_fill_summary}
\alias{tteenrollment_fill_summary}
\title{Report what the follow-up confounder fill changed}
\usage{
tteenrollment_fill_summary(raw, imp)
}
\arguments{
\item{raw}{A \link{TTEEnrollment} object before the fill, or its aggregates
table from \code{\link[=tteenrollment_fill_aggregates]{tteenrollment_fill_aggregates()}}.}

\item{imp}{A \link{TTEEnrollment} object after the fill, or its aggregates table.}
}
\value{
A data.table, one row per confounder, with columns:
\describe{
\item{\code{confounder}}{The confounder name.}
\item{\code{rows_n}}{Rows in the panel.}
\item{\code{rows_filled_n}}{Rows the fill gave a value.}
\item{\code{trials_n}}{Person-trials in the panel.}
\item{\code{trials_filled_n}}{Person-trials the fill left with no \code{NA}.}
\item{\code{trials_entry_imputed_n}}{Person-trials whose snapshot value was
\code{NA} in the raw panel.}
\item{\code{rows_from_imputed_entry_n}}{Rows the imputed snapshot value
reaches.}
}
}
\description{
The function subtracts the aggregates of the filled panel from the
aggregates of the raw panel. The paper reports the result.
}
\details{
Both arguments MUST describe the same panel. The function compares the
\code{key_digest} of the two tables and stops when they differ. It also stops
when the two confounder sets differ. It matches the rows of \code{imp} to the
rows of \code{raw} by confounder name. The two tables MAY therefore list the
same confounders in a different order.
}
\examples{
d <- data.table::data.table(
  enrollment_person_trial_id = rep(c("a", "b"), each = 3L),
  tstart = rep(c(0L, 4L, 8L), 2L),
  tstop = rep(c(4L, 8L, 12L), 2L),
  exposed = rep(c(TRUE, FALSE), each = 3L),
  event = 0L,
  income = c(NA, "mid", NA, NA, NA, "high"),
  .tte_entry__income = rep(c("low", "mid"), each = 3L)
)
design <- TTEDesign$new(
  treatment_var = "exposed",
  outcome_vars = "event",
  confounder_vars = "income",
  follow_up_time = 12L
)
trial <- TTEEnrollment$new(d, design)
raw <- tteenrollment_fill_aggregates(trial)
tteenrollment_fill_followup_confounders(trial)
tteenrollment_fill_summary(raw, trial)

}
\seealso{
\code{\link[=tteenrollment_fill_aggregates]{tteenrollment_fill_aggregates()}} for the input tables.

Other tte_methods:
\code{\link[=tteenrollment_combined_combine]{tteenrollment_combined_combine()}},
\code{\link[=tteenrollment_fill_aggregates]{tteenrollment_fill_aggregates()}},
\code{\link[=tteenrollment_fill_followup_confounders]{tteenrollment_fill_followup_confounders()}},
\code{\link[=tteenrollment_irr_combine]{tteenrollment_irr_combine()}},
\code{\link[=tteenrollment_rates_combine]{tteenrollment_rates_combine()}},
\code{\link[=tteenrollment_rbind]{tteenrollment_rbind()}}
}
\concept{tte_methods}
