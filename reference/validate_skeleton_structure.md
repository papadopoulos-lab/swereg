# Validate skeleton structure

Validate skeleton structure

## Usage

``` r
validate_skeleton_structure(skeleton)
```

## Arguments

- skeleton:

  A data.table that should be a valid skeleton

## Value

Nothing if valid, stops with error if invalid
