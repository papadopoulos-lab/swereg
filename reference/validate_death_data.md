# Validate death registry data columns

Validate death registry data columns

## Usage

``` r
validate_death_data(data)
```

## Arguments

- data:

  A data.table containing death registry data

## Value

Nothing if valid, stops with error if invalid
